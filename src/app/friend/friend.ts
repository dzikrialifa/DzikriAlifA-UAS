export class Friend {
    nama: string;
    emails: string;
    contactno: string;

    constructor(nama, emails, contactno) {
        this.nama = nama;
        this.emails = emails;
        this.contactno = contactno;
    }
}